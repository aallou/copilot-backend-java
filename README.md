# demo-java

Basic Spring Boot project scaffold generated per prompt.

Prerequisites:
- Java 21
- Docker & Docker Compose

Run tests:

```bash
./mvnw clean test
```

Start services and run app:

```bash
docker-compose up -d
./mvnw spring-boot:run
```
